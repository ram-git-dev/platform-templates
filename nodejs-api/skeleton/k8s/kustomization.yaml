apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - servicemonitor.yaml
  - rbac.yaml
  - alerts.yaml
  - team-dashboard.yaml
{% if values.needsDatabase %}
  - database.yaml
{% endif %}
 
